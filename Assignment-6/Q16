#include <stdio.h>

int main() {
	int num, i = 2;

	scanf("%d", &num);

	if (num <= 1) {
		printf("Not Prime");
		return 0;
	}

	while (i <= num / 2) {
		if (num % i == 0) {
			printf("Not Prime");
			return 0;
		}
		i++;
	}

	printf("Prime");
	return 0;
}

